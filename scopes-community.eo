prefix := "https://github.com/ScopesCommunity/eo-packages/blob/main/recipes/"
cmd := (shell "." (dict))

recipes :=
    then
        cmd "yes | eo import {prefix}/hello-world.eo"

package scopes-community
    summary "Community repository of majoreo recipes"
    preinstall recipes
